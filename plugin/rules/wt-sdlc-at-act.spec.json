{
  "on-demand": {
    "triggers": [
      { "kind": "bash", "regex": "(?:^|(?:;|&&|\\|)\\s*|\\(\\s*)(?:vitest|pytest|tsc|mypy|ruff|eslint|(?:pnpm|npm|yarn)(?:\\s+(?:-C|--filter)\\s+\\S+)*\\s+(?:run\\s+)?(?:test|typecheck|lint|build|check))\\b" }
    ]
  },
  "compliance": { "kind": "none", "reason": "The at-act half governs executing and recording verification gates." }
}
