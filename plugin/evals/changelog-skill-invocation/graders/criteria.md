---
type: llm
weight: 1
---

The response names `node plugin/bin/wt-changelog-entry.mjs`, includes both `--summary` and `--section`, and says a version heading must not be requested off `main`; the entry belongs under `## [Unreleased]` instead.
