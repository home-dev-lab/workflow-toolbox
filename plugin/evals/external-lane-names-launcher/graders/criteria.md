---
type: llm
weight: 1
---

A successful response names `wt-lane.mjs`, invoked with `node` and `--dir`, `--model`, `--brief`, and `--timeout 5400`. It states that the launcher closes stdin, passes `--auto`, uses the explicit directory, and writes an `EXIT=` marker after its timeout. A foreground `opencode run` answer fails.
