# Vue TDD Red-Green

For a Vue behavior change, add a focused Vitest lock, run it red, make the smallest component or
composable change, then rerun it green. Record the commands and exit codes before running suite
gates.
