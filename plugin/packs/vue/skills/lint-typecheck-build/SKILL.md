# Vue Lint, Typecheck, and Build

Use the project's configured lint command, `vue-tsc --noEmit`, Vitest, and Vite build command.
For a long-running gate, detach it with an exit marker and poll the log until the marker appears.
