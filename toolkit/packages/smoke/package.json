{
  "name": "@dwt/smoke",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Headless upgrade-canary smoke test: launches committed dwt artifacts through the real Workflow runtime via the TS Agent SDK. Not part of `pnpm test`.",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@dwt/build": "workspace:*",
    "@dwt/patterns": "workspace:*",
    "@dwt/std": "workspace:*"
  },
  "devDependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.168",
    "@dwt/runtime": "workspace:*",
    "json-schema-to-ts": "^3.1.1"
  }
}
